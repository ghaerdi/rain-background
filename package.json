{
  "name": "rain-background",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "serve": "vite preview",
    "format": "prettier --write ./src",
    "prepare": "cp -a ./.githooks/. ./.git/hooks/"
  },
  "devDependencies": {
    "prettier": "^2.4.1",
    "typescript": "^4.3.2",
    "vite": "^2.6.4"
  }
}
